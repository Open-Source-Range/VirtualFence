void SORTx(int MODE_TEMP_ARRAY_X[], int n) {
  //------------------------------------------------------------------0
  for (int j = 0; j < 14; j++) {
    if (MODE_TEMP_ARRAY_X[0] != 0) {
      break;
    }
    else {
      for (int i = 0; i < 14; i++) {
        MODE_TEMP_ARRAY_X[i] = MODE_TEMP_ARRAY_X[i + 1];
      }
    }
  }

  //------------------------------------------------------------------1
  for (int j = 0; j < 14; j++) {
    if (MODE_TEMP_ARRAY_X[1] != 0) {
      break;
    }
    else {
      for (int i = 1; i < 14; i++) {
        MODE_TEMP_ARRAY_X[i] = MODE_TEMP_ARRAY_X[i + 1];
      }
    }
  }

  //------------------------------------------------------------------2
  for (int j = 0; j < 14; j++) {
    if (MODE_TEMP_ARRAY_X[2] != 0) {
      break;
    }
    else {
      for (int i = 2; i < 14; i++) {
        MODE_TEMP_ARRAY_X[i] = MODE_TEMP_ARRAY_X[i + 1];
      }
    }
  }

  //------------------------------------------------------------------3
  for (int j = 0; j < 14; j++) {
    if (MODE_TEMP_ARRAY_X[3] != 0) {
      break;
    }
    else {
      for (int i = 3; i < 14; i++) {
        MODE_TEMP_ARRAY_X[i] = MODE_TEMP_ARRAY_X[i + 1];
      }
    }
  }

  //------------------------------------------------------------------4
  for (int j = 0; j < 14; j++) {
    if (MODE_TEMP_ARRAY_X[4] != 0) {
      break;
    }
    else {
      for (int i = 4; i < 14; i++) {
        MODE_TEMP_ARRAY_X[i] = MODE_TEMP_ARRAY_X[i + 1];
      }
    }
  }

  //------------------------------------------------------------------5
  for (int j = 0; j < 14; j++) {
    if (MODE_TEMP_ARRAY_X[5] != 0) {
      break;
    }
    else {
      for (int i = 5; i < 14; i++) {
        MODE_TEMP_ARRAY_X[i] = MODE_TEMP_ARRAY_X[i + 1];
      }
    }
  }

  //------------------------------------------------------------------6
  for (int j = 0; j < 14; j++) {
    if (MODE_TEMP_ARRAY_X[6] != 0) {
      break;
    }
    else {
      for (int i = 6; i < 14; i++) {
        MODE_TEMP_ARRAY_X[i] = MODE_TEMP_ARRAY_X[i + 1];
      }
    }
  }

  //------------------------------------------------------------------7
  for (int j = 0; j < 14; j++) {
    if (MODE_TEMP_ARRAY_X[7] != 0) {
      break;
    }
    else {
      for (int i = 7; i < 14; i++) {
        MODE_TEMP_ARRAY_X[i] = MODE_TEMP_ARRAY_X[i + 1];
      }
    }
  }

  //------------------------------------------------------------------8
  for (int j = 0; j < 14; j++) {
    if (MODE_TEMP_ARRAY_X[8] != 0) {
      break;
    }
    else {
      for (int i = 8; i < 14; i++) {
        MODE_TEMP_ARRAY_X[i] = MODE_TEMP_ARRAY_X[i + 1];
      }
    }
  }

  //------------------------------------------------------------------9
  for (int j = 0; j < 14; j++) {
    if (MODE_TEMP_ARRAY_X[9] != 0) {
      break;
    }
    else {
      for (int i = 9; i < 14; i++) {
        MODE_TEMP_ARRAY_X[i] = MODE_TEMP_ARRAY_X[i + 1];
      }
    }
  }

  //------------------------------------------------------------------10
  for (int j = 0; j < 14; j++) {
    if (MODE_TEMP_ARRAY_X[10] != 0) {
      break;
    }
    else {
      for (int i = 10; i < 14; i++) {
        MODE_TEMP_ARRAY_X[i] = MODE_TEMP_ARRAY_X[i + 1];
      }
    }
  }

  //------------------------------------------------------------------11
  for (int j = 0; j < 14; j++) {
    if (MODE_TEMP_ARRAY_X[11] != 0) {
      break;
    }
    else {
      for (int i = 11; i < 14; i++) {
        MODE_TEMP_ARRAY_X[i] = MODE_TEMP_ARRAY_X[i + 1];
      }
    }
  }

  //------------------------------------------------------------------12
  for (int j = 0; j < 14; j++) {
    if (MODE_TEMP_ARRAY_X[12] != 0) {
      break;
    }
    else {
      for (int i = 12; i < 14; i++) {
        MODE_TEMP_ARRAY_X[i] = MODE_TEMP_ARRAY_X[i + 1];
      }
    }
  }
}
